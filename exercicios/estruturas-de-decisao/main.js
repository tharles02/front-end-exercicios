// do while

/*
let nome;

do {
    nome = prompt("qual o seu nome");
} while (!nome);
console.log("ola" + nome );
*/

/*
let numero;

do {
    numero = prompt("insira um numero");
}while (numero != 1 && numero != 0);
*/

/*
let senhaArmazenada = 123456
let senha

do {
    senha = prompt("insira sua senha");
}while (senha !=senhaArmazenada);
console.log("senha correta")
*/

/*
let numero 
let numeroMaior = 0;
let contador = 0;

do {
    numero = prompt("insira o numero");
    numero = Number(numero);
    contador = contador + 1;
    if (numero > numeroMaior) {
        numeroMaior = numero;
    }
}while (contador <= 4);
    console.log(numeroMaior);
*/

// for 
/*
for ( let contador = 0; contador < 100; contador = contador + 1 ) {
    console.log(contador)
}
*/

let calcularPesomedio = 0;
let pesoPessoais = 50;
let pessoais = 10;

do {
    pesoPessoais = prompt('insira seu peso');
    pesoPessoais = Number(numero);
    calcularPesomedio = calcularPesomedio + 1;
    if(pesoPessoais > pessoais) {
        Pessoais = pesoPessoais;
    }
} while (calcularPesomedio <= 50);

for ( let pessoais = 10; pessoais < 10; pessoais = pessoais + 1 ) {
    console.log(pessoais)
}